{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://actl.example/protocol/message-error.schema.json",
  "title": "actl.acp/1 error message body",
  "description": "§14 message table's \"error\" row: reason_code, human message, retryable flag. The body of both a plain (unsigned, pre-verification) protocol rejection and a signed AgentEnvelope whose type is \"error\".",
  "type": "object",
  "additionalProperties": false,
  "required": ["reason_code", "message", "retryable"],
  "properties": {
    "reason_code": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" },
    "message": { "type": "string" },
    "retryable": { "type": "boolean" }
  }
}
